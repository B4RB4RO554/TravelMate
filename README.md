# TravelMate
basic travel app
for the serer part if you want to clone it use the folowing commands
    npm install express dotenv
    npm install bcryptjs jsonwebtoken
    npm install axios
 then run it using the command
    npx nodemon src/server.js
